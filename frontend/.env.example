# Clerk — frontend publishable key (safe to expose in client bundle).
# Get it from the Clerk dashboard. Without it the app runs in auth-disabled mode.
# Copy this file to `.env.local` and fill in the value.
VITE_CLERK_PUBLISHABLE_KEY=
